jdbc.driverClassName=com.mysql.jdbc.Driver
jdbc.url=jdbc:mysql://127.0.0.1:3306/lakerhome
jdbc.username=root
jdbc.password=mysql

# 生成实体类所在的包
package.model=com.ciper.lakerhome.entity
# 生成 mapper 类所在的包
package.mapper=com.ciper.lakerhome.mapper
# 生成 mapper xml 文件所在的包，默认存储在 resources 目录下
package.xml=mapper